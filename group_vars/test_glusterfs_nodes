---
lvm_volume_groups:
  gluster_vg_0:
    physical_volumes:
      - /dev/vda

lvm_logical_volumes:
  glusterfs_storage:
    vg: glusterfs_vg
    size: 10G

filesystems:
  - device: /dev/glusterfs_vg/glusterfs_storage
    mountpoint: /mnt/bricks/brick_0
    fs: ext4

glusterfs_volumes:
  replicated_volumes:   
    - name: gv50
      replicas: 2
      bricks:
        - /mnt/bricks/brick_0

proxmox_mount_volumes:
  - server1: {{ groups[target+'_glusterfs_nodes'][0] }}
    server2: 
    volume_name: gv50
    id: glusterfs_storage
