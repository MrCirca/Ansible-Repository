{% for radius_client in freeradius_clients %}
client {{ radius_client.name }} {
        ipaddr  = {{radius_client.ip_address}}
        secret  = {{radius_client.secret}}
        nastype = {{radius_client.type}}

        }
{% endfor %}        
